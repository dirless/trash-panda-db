name: trash-panda-db
version: 0.1.0

authors:
  - lampros.chaidas@gmail.com

description: |
  A pure Crystal embedded database with Raft replication and crystal-db compatibility.
  No C bindings. No system library dependencies.

dependencies:
  db:
    github: crystal-lang/crystal-db
    version: "~> 0.14"

crystal: ">= 1.20.0"

license: MIT
